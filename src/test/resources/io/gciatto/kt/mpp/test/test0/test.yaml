tests:
  - description: "Root project should be multiplatform"
    configuration:
      tasks: ":tasks"
      options: "--stacktrace"
    expectation:
      output_contains: Tasks runnable from root project 'test0'
      success: ':tasks'
