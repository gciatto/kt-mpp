tests:
  - description: "Root project should be multiplatform"
    configuration:
      tasks: ":tasks"
      options:
        - "--stacktrace"
    expectation:
      success: ':tasks'
      output_contains:
        - Tasks runnable from root project 'test0'

