options:
  - "--stacktrace"
tests:
  - description: "Projects are auto-assigned with type correctly"
    configuration:
      tasks:
        - "test"
      options:
        - "--info"
    expectation:
      success:
        - ':full:jvmTest'
        - ':full:jsTest'
      output_contains:
        - "full: set Mocha per-test-case timeout to 180s"
